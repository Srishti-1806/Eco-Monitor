# Ecomonitor

## ThingSpeak Integration

To use ThingSpeak IoT water monitoring data, set these environment variables:

- `THINGSPEAK_CHANNEL_ID`
- `THINGSPEAK_READ_KEY`

The app will fetch the latest ThingSpeak feed from `/api/thingspeak` and display live water sensor values on the Water Quality page.

